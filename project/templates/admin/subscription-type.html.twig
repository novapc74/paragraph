<div class="d-flex flex-column text-center py-3">
    <div class="bg-secondary">
        <p class="h5 mx-5 my-2 bg-secondary">это всё, чем располагаем</p>
    </div>
    <div>
        <ul class="list-group">
            {% for email in emails %}
                <li class="list-group-item">{{ email }}</li>
            {% endfor %}
        </ul>
    </div>
</div>

